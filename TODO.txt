
xxx.route.ts 
    string(id) instead of WrapperType !!!
    because type is "uuid" ???
      ( id : uuid  )
    Example:
        export const drivinglicenseResolver: ResolveFn<DrivingLicense> = (route: ActivatedRouteSnapshot) => {
        const service = inject(DrivingLicenseService);
        // Get Primary Key elements 
        const id = route.paramMap.get('id'); 
        // Call service for Primary Key 
        return service.getDrivingLicense(string(id)); // PK
        };